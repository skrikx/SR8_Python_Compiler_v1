{
  "source_terms": ["eval module", "benchmark corpus loader", "regression comparison"],
  "required_fields": ["objective", "scope", "constraints", "dependencies", "output_contract"],
  "constraint_terms": ["cli stable", "network services"],
  "required_transform_targets": ["markdown_plan", "markdown_prompt_pack"],
  "trust_field_statuses": {
    "objective": "explicit",
    "scope": "explicit",
    "dependencies": "explicit",
    "target_class": "explicit"
  },
  "expect_human_review": false,
  "minimum_readiness": "pass"
}
